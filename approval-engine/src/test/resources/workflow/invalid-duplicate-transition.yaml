name: broken
version: 1
initialState: SUBMITTED
terminalStates: [APPROVED]
states:
  - id: SUBMITTED
    label: Submitted
  - id: APPROVED
    label: Approved
transitions:
  - name: approve
    from: SUBMITTED
    to: APPROVED
    guard: no_approval_required
  - name: approve
    from: SUBMITTED
    to: APPROVED
    guard: approval_required
